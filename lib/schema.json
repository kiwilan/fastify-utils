{
  "description": "Core schema meta-schema",
  "type": "object",
  "properties": {
    "dotenv": {
      "type": "object",
      "description": "A list of environment variables to load from .env files",
      "additionalProperties": {
        "enum": ["string", "boolean", "array", "object"]
      }
    }
  },
  "default": {}
}
